This tool holds extra m4 files used by autoreconf to generate aclocal.m4. 
At the moment these are the libtoolize copied files that should be supplied
with source distributions.  To add or refresh the libtool files:

	$ cd /directory/with/configure
	$ libtoolize --copy --install

-END-
