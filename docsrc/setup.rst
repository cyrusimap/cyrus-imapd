.. _setup:

=====
Setup
=====

So you want to set up Cyrus.

You'll need to `compile <developer/compiling>`_ it, `install <installing>`_ it,
maybe `upgrade from the last release <download/upgrade>`_, and `deploy
<concepts/deployment>`_ it.
