Name: lmtp_over_quota_perm_failure
Type: SWITCH
Default-Value: 0
Last-Modified: 2.3.17

If enabled, lmtpd returns a permanent failure code when a user's mailbox is
over quota.  By default, the failure is temporary, causing the MTA to queue
the message and retry later.
