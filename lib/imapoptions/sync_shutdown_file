Name: sync_shutdown_file
Type: STRING
Default-Value: NULL
Last-Modified: 2.5.0

Simple latch used to tell sync_client(8) that it should shut down at the
next opportunity. Safer than sending signals to running processes.  Prefix
with a channel name to only apply for that channel.
