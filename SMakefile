#
# This file is specific to Carnegie Mellon University's
# software build process
#

MAKE=gmake

AMSSYNC=--enable-amssync

WITHAUTH="--with-auth=krb_pts"
if test `sys` = pmax_ul4; then
	WITHAUTH=""
	AMSSYNC=""
fi

LNCOMMAND="${SRCDIR}/configure $WITHAUTH --with-inn=/usr/news $AMSSYNC --enable-cmulocal --with-cyrus-user=$USER --with-cyrus-group=staff"
