Option: archive_maxsize
Type: BYTESIZE
Default: 1024K
Last-Modified: 3.8.0

The size of the largest message that won't be archived immediately.

For backward compatibility, if no unit is specified, kibibytes is
assumed.
