Description:

Adds support to expire JMAP notifications using cyr_expire.

Config changes:

The new "--jmap-notif" option of cyr_expire allows to set the maximum
age of JMAP notifications before they get deleted. The only JMAP
notification objects currently generated by Cyrus is
CalendarEventNotification objects.
The new "--longlock" option allows to specify the maximum mailbox lock
duration when deleting JMAP notifications.

Upgrade instructions:

None.

GitHub issue:

None.
