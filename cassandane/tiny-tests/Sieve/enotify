#!perl
use Cassandane::Tiny;

sub test_enotify
    :min_version_3_2
    ($self)
{
    $self->{instance}->install_sieve_script(<<'EOF'
require ["enotify", "body"];

notify "https://cyrusimap.org/notifiers/updatecal";
notify :message "$text[5]$ World!" "mailto:foo@example.com";
EOF
        );

    xlog $self, "Deliver a message";
    my $msg1 = $self->{gen}->generate(subject => "Message 1",
                                      from => Cassandane::Address->new(
                                          localpart => 'text',
                                          domain => 'example.com'
                                      ),
                                      body => 'Hello Ms. Cassandane');
    $self->{instance}->deliver($msg1);

    my $data = $self->{instance}->getnotify();
    my ($updatecal) = grep { $_->{METHOD} eq 'updatecal' } @$data;
    my ($mailto) = grep { $_->{METHOD} eq 'mailto' } @$data;

    $self->assert_not_null($updatecal);
    $self->assert_matches(qr/<text\@example.com>: Message 1/,
                          $updatecal->{MESSAGE});
    $self->assert_not_null($mailto);
    $self->assert_matches(qr/Hello World!/, $mailto->{MESSAGE});
}
